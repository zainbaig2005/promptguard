# Evaluator Design

Evaluators are deterministic by default and return outcome, confidence, concise reasoning, evaluator identity, version, and metadata. Optional probabilistic judges should be disabled by default.
