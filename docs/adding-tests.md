# Adding Tests

Add YAML entries under `data/test_suites/`. Each test needs a stable ID, OWASP category, assessment mode, prompt, expected behavior, evaluator, severity, tags, authorization flag, timeout, and source classification.
